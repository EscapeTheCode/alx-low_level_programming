#include <stdio.h>
/**
 * main - entry
 *
 * Return: 0 (success)
 */
int main(void)

{
	char lower_case;
	char upper_case;

	for (upper_case = 'A'; upper_case <= 'Z'; upper	_case++)
	{
putchar(upper_case);
	}
	for (lower_case) = 'a'; lower_case <= 'z'; lowercase++)
	{
putchar(lower_case);
	}

putchar('\n');

return (0);

}

